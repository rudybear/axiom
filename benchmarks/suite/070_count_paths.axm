@module count_paths;

@pure
fn count_paths(m: i32, n: i32) -> i32 {
    if m == 1 {
        return 1;
    }
    if n == 1 {
        return 1;
    }
    return count_paths(m - 1, n) + count_paths(m, n - 1);
}

fn main() -> i32 {
    print_i32(count_paths(10, 10));
    return 0;
}
