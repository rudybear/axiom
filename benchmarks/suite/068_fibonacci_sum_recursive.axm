@module fibonacci_sum_recursive;

@pure
fn fib(n: i32) -> i32 {
    if n <= 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() -> i32 {
    let sum: i32 = 0;
    for i: i32 in range(0, 31) {
        sum = sum + fib(i);
    }
    print_i32(sum);
    return 0;
}
