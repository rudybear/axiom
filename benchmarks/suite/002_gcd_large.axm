@module gcd_large;

@pure
fn gcd(a: i32, b: i32) -> i32 {
    let x: i32 = a;
    let y: i32 = b;
    while y != 0 {
        let t: i32 = y;
        y = x % y;
        x = t;
    }
    return x;
}

fn main() -> i32 {
    print_i32(gcd(123456, 7890));
    return 0;
}
