@module triangular_number;

@pure
fn triangular(n: i32) -> i32 {
    let sum: i32 = 0;
    for i: i32 in range(1, n + 1) {
        sum = sum + i;
    }
    return sum;
}

fn main() -> i32 {
    print_i32(triangular(100));
    return 0;
}
