@module tak;

@pure
fn tak(x: i32, y: i32, z: i32) -> i32 {
    if y >= x {
        return z;
    }
    return tak(tak(x - 1, y, z), tak(y - 1, z, x), tak(z - 1, x, y));
}

fn main() -> i32 {
    print_i32(tak(18, 12, 6));
    return 0;
}
