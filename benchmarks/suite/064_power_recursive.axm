@module power_recursive;

@pure
fn power(base: i32, exp: i32) -> i32 {
    if exp == 0 {
        return 1;
    }
    return base * power(base, exp - 1);
}

fn main() -> i32 {
    print_i32(power(3, 10));
    return 0;
}
