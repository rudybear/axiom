@module gcd_recursive_stress;

@pure
fn gcd(a: i32, b: i32) -> i32 {
    if b == 0 {
        return a;
    }
    return gcd(b, a % b);
}

fn main() -> i32 {
    let result: i32 = 0;
    for i: i32 in range(1, 1001) {
        result = gcd(i, 997);
    }
    print_i32(result);
    return 0;
}
