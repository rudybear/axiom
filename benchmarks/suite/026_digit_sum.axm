@module digit_sum;

@pure
fn digit_sum(n: i32) -> i32 {
    let sum: i32 = 0;
    let x: i32 = n;
    while x > 0 {
        sum = sum + x % 10;
        x = x / 10;
    }
    return sum;
}

fn main() -> i32 {
    print_i32(digit_sum(123456789));
    return 0;
}
