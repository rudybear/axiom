@module fibonacci_recursive_35;

@pure
fn fib(n: i32) -> i32 {
    if n <= 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() -> i32 {
    print_i32(fib(35));
    return 0;
}
