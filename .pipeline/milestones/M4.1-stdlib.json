{
  "id": "M4.1-stdlib",
  "name": "Standard Library",
  "description": "Core standard library: math functions, string operations, I/O primitives, array/slice utilities, and basic tensor operations as built-in functions recognized by the compiler.",
  "depends_on": ["M3.2-multi-agent-demo"],
  "crates_affected": ["axiom-codegen", "axiom-hir", "axiom-driver"],
  "status": "not_started",
  "acceptance_criteria": [
    {
      "id": "AC-4.1.1",
      "description": "Math builtins (abs, min, max, sqrt, pow) work",
      "check_command": "cargo test -p axiom-codegen test_math_builtins 2>&1",
      "check_pattern": "test result: ok",
      "check_type": "command_output_contains"
    },
    {
      "id": "AC-4.1.2",
      "description": "All workspace tests pass",
      "check_command": "cargo test --workspace 2>&1",
      "check_pattern": "failures",
      "check_type": "command_output_not_contains"
    }
  ],
  "benchmark_criteria": []
}
