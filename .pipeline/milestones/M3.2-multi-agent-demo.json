{
  "id": "M3.2-multi-agent-demo",
  "name": "Multi-Agent Demo",
  "description": "Demonstrate multi-agent workflow: Agent A writes naive code, Agent B optimizes for CPU, handoff via @transfer blocks with optimization history preserved.",
  "depends_on": ["M3.1-transfer-protocol"],
  "crates_affected": ["axiom-optimize", "axiom-driver"],
  "status": "not_started",
  "acceptance_criteria": [
    {
      "id": "AC-3.2.1",
      "description": "Multi-agent example exists and compiles",
      "check_command": "cargo test -p axiom-optimize test_multi_agent 2>&1",
      "check_pattern": "test result: ok",
      "check_type": "command_output_contains"
    },
    {
      "id": "AC-3.2.2",
      "description": "All workspace tests pass",
      "check_command": "cargo test --workspace 2>&1",
      "check_pattern": "failures",
      "check_type": "command_output_not_contains"
    }
  ],
  "benchmark_criteria": []
}
