{
  "id": "M4.2-interop",
  "name": "C Interop & FFI",
  "description": "Call C functions from AXIOM via extern declarations. Generate C-compatible function exports from AXIOM. Enable linking with C libraries.",
  "depends_on": ["M4.1-stdlib"],
  "crates_affected": ["axiom-codegen", "axiom-parser", "axiom-hir", "axiom-driver"],
  "status": "not_started",
  "acceptance_criteria": [
    {
      "id": "AC-4.2.1",
      "description": "extern function declarations compile to LLVM IR declarations",
      "check_command": "cargo test -p axiom-codegen test_extern_decl 2>&1",
      "check_pattern": "test result: ok",
      "check_type": "command_output_contains"
    },
    {
      "id": "AC-4.2.2",
      "description": "All workspace tests pass",
      "check_command": "cargo test --workspace 2>&1",
      "check_pattern": "failures",
      "check_type": "command_output_not_contains"
    }
  ],
  "benchmark_criteria": []
}
