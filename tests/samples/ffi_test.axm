@module ffi_test;
@intent("Test Rust-style FFI: calling convention + @link annotation");

@link("msvcrt")
extern "C" fn _time64(t: ptr[i64]) -> i64;

extern "C" fn puts(s: ptr[i8]) -> i32;

fn main() -> i32 {
    return 0;
}
